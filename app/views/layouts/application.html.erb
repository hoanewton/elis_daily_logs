<!DOCTYPE html>
<html>
<head>
  <title>ElisDailyLog</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

	<!-- bootstrap navbar -->
  <nav class="navbar navbar-inverse navbar-static-top" role="navigation">
    <div class="container">
      <ul class=" nav navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to('Home', root_path)%></li>
          <li><%= link_to('Sign Out', destroy_user_session_path, method: :delete)%></li>
          <li><%= link_to('Edit Account', edit_user_registration_path, :id => 'edit-user') %></li>
          <li><%= link_to('Babies', babies_path)%></li>
          <p class="navbar-text">Welcome back, <%= current_user.first_name %>!</p>
        <% else %>
          <li ><%= link_to('Sign In', new_user_session_path) %></li>
          <li><%= link_to('Sign Up', new_user_registration_path) %></li>
        <% end %>
          
      </ul>
    </div>
  </nav>

	<div class='container'>
		<h5 class="notice"><%= notice %></h5>
	  <h5 class="alert red"><%= alert %></h5>
		<%= yield %>
	</div>

</body>
</html>
